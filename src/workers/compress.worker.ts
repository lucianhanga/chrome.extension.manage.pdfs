// OffscreenCanvas image (re)compression worker (Phase 4 implementation).
// Runs off the main thread. Receives image bytes and compression params;
// returns re-encoded bytes.

// TODO Phase 4: implement OffscreenCanvas downsample + JPEG re-encode.

self.onmessage = (_event: MessageEvent) => {
  // Placeholder — will handle compression tasks in Phase 4.
};
