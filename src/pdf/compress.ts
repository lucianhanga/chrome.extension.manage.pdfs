// Image compression dispatch to OffscreenCanvas worker (Phase 4 implementation).
// Stub only.

// TODO Phase 4:
//   export async function compressImage(
//     imageBytes: ArrayBuffer,
//     mimeType: string,
//     params: ExportProfileParams,
//   ): Promise<ArrayBuffer>
//
//   Dispatches to compress.worker.ts via Worker postMessage.
